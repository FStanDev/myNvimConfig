local options = {
  ensure_installed = { "lua","rust" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },

  rust = {
    enable = true
  },
}

return options
